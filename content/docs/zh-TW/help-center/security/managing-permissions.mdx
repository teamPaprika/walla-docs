---
title: 設定權限
description: 依團隊與工作區的角色（擁有者／管理者／成員）設定可使用的功能與權限。
---

import { Callout } from 'fumadocs-ui/components/callout';

## 設定團隊成員權限

團隊角色的順序為擁有者 > 管理者 > 成員。
團隊擁有者可以在 **成員管理** 頁面下方的 `角色權限` 區塊，以開關切換 **管理者** 與 **成員** 角色的細部權限。

## 設定工作區成員權限
在工作區點選 `⚙️` 按鈕前往工作區設定，然後在角色與權限分頁設定管理者與成員的權限。
