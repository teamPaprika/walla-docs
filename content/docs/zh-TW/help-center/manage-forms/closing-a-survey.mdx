---
title: "結束問卷"
description: "想結束問卷，或限制填答人數以先到先得的方式截止時，可以使用以下兩種方法。"
---

## 手動結束問卷

- 不想再收集回覆時，點選畫面右上角的 `⚙️` 按鈕，把「接收回覆」開關關掉。
- 關閉後會立即停止接收回覆，參與者即使進入問卷也無法送出。

## 用回覆數上限做先到先得截止

- 專案編輯畫面 → 點選右上角的 `⚙️` 按鈕開啟設定選單。
- 輸入要限制的回覆數（例如 10 人）。不必按發布就會立即生效。
- 達到設定的回覆數後，後續參與者就無法送出問卷。例如把上限設為 10 人時，第 11 位填答者就無法參與。
